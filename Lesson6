

#include <stdio.h>

#include <math.h>



using namespace std;
#define Max_Size 150
static int g_a [n];

int main()

{

  int i, n, z, sup, inf, average, xi;

  scanf("%d\n", &n);
    if (n<=Max_Size)

        for (i=0; i<= n-1; i++)

        {

            scanf("%d\n", &z);

            a[i]= z;

        }

        sup= a[0];

            for (i=2; i<=n-1; i++)

            {

                if (a[i]>a[i-1])

                sup=a[i];

            }

    

    

  

    

    

}
